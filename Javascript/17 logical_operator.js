// Logical Operator

// &&
// ||
// !

let a=0;
let b=5;

console.log((a>5) && (a<5)); // false and true      --->      false
console.log((a>b) || (a<b)); // false or  true      --->      true
console.log(!(a>b)); //not false        --->      true