
function profile(){
          let name="thhs";
          let age=17;
}

// console.log(name); // We can't use the variables built in function.

class Profile{
          name ="thhs";
          age = 20;
}

// creating object for class Profile
let myProfile = new Profile;
// displaying data from class Profile
console.log(myProfile.name);
console.log(myProfile.age);// We can use the varibles built in class