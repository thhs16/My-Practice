let b=0;
for(let a=0; a<10; a++){
          // ++b;
          // console.log("Process is started.",b)
          let c=0
          for(c; c<a; c++){
                    // console.log(arr.join('*'));
                    process.stdout.write(`${'*'}`);
          }
          console.log("\n");
}